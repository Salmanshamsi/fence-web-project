import { createSlice } from '@reduxjs/toolkit';

const initialState = {};

export const continueBtnHandling = createSlice({
  name: 'continueBtnHandling',
  initialState,
  reducers: {

  },
});

// Action creators are generated for each case reducer function
export const {  } = continueBtnHandling.actions;

export default continueBtnHandling.reducer;
